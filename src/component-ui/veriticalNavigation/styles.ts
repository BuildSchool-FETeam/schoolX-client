import { IStyleSheet } from 'theme/interfaces';

export const styles: IStyleSheet = {
  root: {
    minH: '3rem',
    minW: '16.25rem',
    alignItems: 'center',
    px: '.75rem',
    cursor: 'pointer',

    _hover: {
      bgColor: 'rgba()'
    }
  }
};
